import { NONAME } from "dns";

const style = () => ({
	input: {
		maxWidth: '500px',
	},
	'select, input[name="stock"]': {
		maxWidth: '200px',
	},
	label: {
		textTransform: 'capitalize'
	},
	'#submit-button': {
		display: 'none'
	}
});

export default style;
