const style = () => ({
    'input, select, .alert': {
        maxWidth: '500px',
    },
    '.alert': {
        fontWeight: 'bold',
        backgroundColor: 'var(--white)',
        borderColor: 'var(--primary)',
        color: 'var(--primary)',
    },
});

export default style;
