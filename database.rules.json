{
  "rules": {
    "displayNames": {
     ".read": true,
     "$uid": {
        ".write": "auth !== null && auth.uid === $uid"
    	}
    },
    "requestFormStatus": {
     ".read": true,
     ".write": "auth !== null"
    }
  }
}